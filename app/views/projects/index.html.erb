<%= render "shared/header" %>

<section id="portfolio">
  <h2 class="section-heading">Réalisations<br><em>récentes</em></h2>
  <div class="projects-list">
    <% @projects.each do |project| %>
      <%= render "projects/card", project: project %>
    <% end %>
  </div>
</section>